---
import Layout from '../../layouts/Layout.astro';
import { CreateEventForm } from '../../components/CreateEventForm';

if (!Astro.locals.user) {
  return Astro.redirect('/login');
}
---

<Layout title="Crear Evento">
  <div class="flex min-h-screen items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-lg bg-white p-8 rounded-xl shadow-md space-y-8">
      <div>
        <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">Crear Nuevo Evento</h2>
        <p class="mt-2 text-center text-sm text-gray-600">
          Ingresa el nombre del evento y carga 2 imágenes.
        </p>
      </div>
      
      <CreateEventForm client:load />
    </div>
  </div>
</Layout>
